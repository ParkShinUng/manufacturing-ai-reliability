# Measured Results

> **The implementation gate is CLOSED.** This directory is a documented placeholder. It must
> contain no executable artifact until `docs/10-human-review/v0.3/HUMAN_APPROVAL.md` records
> approval. A README stating intent is documentation; a manifest that would run is not.

**This directory is empty on purpose, and that is the honest state of the project.**

Every performance figure in the specification is labelled `TARGET (unmeasured)`. A target becomes a result only when a report lands here carrying git commit, deployment profile, machine specification, duration, configuration, observed metrics, and pass/fail against its AC ID.

| Subdirectory | Contents | Phase |
|---|---|---|
| `load/` | LOAD-001..005 | 12 |
| `failure/` | the 13 FAIL-* tests | 12 |

Until a file appears here, **no number in this repository may be presented as measured** (NFR-011, AC-042).
